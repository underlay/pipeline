import React from "react";
const CollectionExportEditor = {
    name: "Collection Export",
    component({ state, setState }) {
        return React.createElement("div", null);
    },
};
export default CollectionExportEditor;
