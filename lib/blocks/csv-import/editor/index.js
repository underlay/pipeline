import React from "react";
const CsvImportEditor = {
    name: "CSV Import",
    backgroundColor: "lavender",
    component({ state, setState }) {
        return React.createElement("div", null);
    },
};
export default CsvImportEditor;
